name: Deploy Staging

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write
  id-token: write
  security-events: write

jobs:
  build-and-push:
    timeout-minutes: 30
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set lowercase owner
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Backup current staging-latest as staging-previous
        run: |
          set -e
          for img in backend frontend; do
            REPO=ghcr.io/${{ env.OWNER_LC }}/zizalend-${img}
            if timeout 30 docker pull ${REPO}:staging-latest; then
              docker tag ${REPO}:staging-latest ${REPO}:staging-previous || true
              timeout 30 docker push ${REPO}:staging-previous || true
            fi
          done

      - name: Build and push backend
        uses: docker/build-push-action@v7
        with:
          context: ./backend
          push: true
          tags: |
            ghcr.io/${{ env.OWNER_LC }}/zizalend-backend:staging-${{ github.sha }}
            ghcr.io/${{ env.OWNER_LC }}/zizalend-backend:staging-latest
          target: production
          build-args: |
            GIT_SHA=${{ github.sha }}
            BUILD_TIME=${{ github.event.head_commit.timestamp }}

      - name: Build and push frontend
        uses: docker/build-push-action@v7
        with:
          context: ./frontend
          push: true
          tags: |
            ghcr.io/${{ env.OWNER_LC }}/zizalend-frontend:staging-${{ github.sha }}
            ghcr.io/${{ env.OWNER_LC }}/zizalend-frontend:staging-latest
          target: runner
          build-args: |
            NEXT_PUBLIC_API_URL=${{ vars.NEXT_PUBLIC_API_URL }}
            NEXT_PUBLIC_RPC_URL=${{ vars.NEXT_PUBLIC_RPC_URL }}
            NEXT_PUBLIC_NETWORK_PASSPHRASE=${{ vars.NEXT_PUBLIC_NETWORK_PASSPHRASE }}
            NEXT_PUBLIC_EXPLORER_URL=${{ vars.NEXT_PUBLIC_EXPLORER_URL }}
            NEXT_PUBLIC_MANAGER_CONTRACT_ID=${{ vars.NEXT_PUBLIC_MANAGER_CONTRACT_ID }}
            NEXT_PUBLIC_POOL_CONTRACT_ID=${{ vars.NEXT_PUBLIC_POOL_CONTRACT_ID }}
            NEXT_PUBLIC_GOVERNANCE_CONTRACT_ID=${{ vars.NEXT_PUBLIC_GOVERNANCE_CONTRACT_ID }}
            NEXT_PUBLIC_NFT_CONTRACT_ID=${{ vars.NEXT_PUBLIC_NFT_CONTRACT_ID }}

      - name: Run Trivy vulnerability scanner (HIGH - warn)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'ghcr.io/${{ env.OWNER_LC }}/zizalend-backend:staging-${{ github.sha }}'
          format: 'table'
          severity: 'HIGH'
          exit-code: '0'
          trivyignores: '.trivyignore'

      - name: Run Trivy vulnerability scanner (CRITICAL - fail)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'ghcr.io/${{ env.OWNER_LC }}/zizalend-backend:staging-${{ github.sha }}'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL'
          exit-code: '1'
          # Without this, trivy-action builds the SARIF with ALL severities and
          # the exit-code then trips on any vulnerability (e.g. the existing
          # HIGH axios advisories) — not just CRITICAL as the step intends.
          # Limiting the report to the requested severity makes the gate fail
          # only on genuine CRITICAL findings.
          limit-severities-for-sarif: 'true'
          trivyignores: '.trivyignore'

      - name: Check if backend SARIF exists
        id: check-backend-sarif
        if: always()
        run: |
          if [ -f trivy-results.sarif ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        if: always() && steps.check-backend-sarif.outputs.exists == 'true'
        with:
          sarif_file: 'trivy-results.sarif'
          category: backend

      - name: Run Trivy vulnerability scanner for frontend (HIGH - warn)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'ghcr.io/${{ env.OWNER_LC }}/zizalend-frontend:staging-${{ github.sha }}'
          format: 'table'
          severity: 'HIGH'
          exit-code: '0'
          trivyignores: '.trivyignore'

      - name: Run Trivy vulnerability scanner for frontend (CRITICAL - warn)
        # exit-code set to 0 because node:22-alpine base image frequently has
        # CRITICAL CVEs in upstream Alpine packages (e.g. busybox, libssl).
        # These are tracked via SARIF upload to GitHub Security tab rather
        # than blocking deployment, which would create churn without an
        # actionable fix on our side.
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'ghcr.io/${{ env.OWNER_LC }}/zizalend-frontend:staging-${{ github.sha }}'
          format: 'sarif'
          output: 'frontend-trivy-results.sarif'
          severity: 'CRITICAL'
          exit-code: '0'
          limit-severities-for-sarif: 'true'
          trivyignores: '.trivyignore'

      - name: Check if frontend SARIF exists
        id: check-frontend-sarif
        if: always()
        run: |
          if [ -f frontend-trivy-results.sarif ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Upload frontend Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        if: always() && steps.check-frontend-sarif.outputs.exists == 'true'
        with:
          sarif_file: 'frontend-trivy-results.sarif'
          category: frontend

      - name: Smoke-test the backend staging image
        run: |
          set -e
          REPO=ghcr.io/${{ env.OWNER_LC }}/zizalend-backend
          timeout 60 docker pull "${REPO}:staging-${{ github.sha }}"
          IMG_ID=$(docker image inspect "${REPO}:staging-${{ github.sha }}" --format '{{.Id}}')
          echo "Image pulled successfully: $IMG_ID"
          echo "Backend staging image smoke-test passed (image exists and is pullable)."

  deploy:
    needs: build-and-push
    if: ${{ vars.STAGING_ENABLED == 'true' }}
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set lowercase owner
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - name: Prepare staging compose with owner and TAG
        run: |
          mkdir -p deploy
          sudo apt-get update && sudo apt-get install -y gettext-base
          export OWNER=${{ env.OWNER_LC }}
          export TAG=staging-latest
          envsubst < docker-compose.staging.yml > deploy/docker-compose.staging.resolved.yml
          # pre-generate a rollback compose referencing staging-previous
          export TAG=staging-previous
          envsubst < docker-compose.staging.yml > deploy/docker-compose.staging.rollback.yml

      - name: Copy compose files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.STAGING_SSH_HOST }}
          username: ${{ secrets.STAGING_SSH_USER }}
          key: ${{ secrets.STAGING_SSH_KEY }}
          port: ${{ secrets.STAGING_SSH_PORT || 22 }}
          source: "docker-compose.yml,deploy/docker-compose.staging.resolved.yml,deploy/docker-compose.staging.rollback.yml"
          target: "~/ZizaLend"

      - name: Deploy on server and run migrations + healthcheck
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.STAGING_SSH_HOST }}
          username: ${{ secrets.STAGING_SSH_USER }}
          key: ${{ secrets.STAGING_SSH_KEY }}
          port: ${{ secrets.STAGING_SSH_PORT || 22 }}
          script: |
            set -e
            cd ~/ZizaLend
            docker-compose -f docker-compose.yml -f docker-compose.staging.resolved.yml pull

            # Start DB first and wait for readiness
            docker-compose -f docker-compose.yml -f docker-compose.staging.resolved.yml up -d db
            for i in $(seq 1 30); do
              if docker-compose -f docker-compose.yml -f docker-compose.staging.resolved.yml exec -T db pg_isready -U postgres -d ZizaLend >/dev/null 2>&1; then
                echo "DB is ready"
                break
              fi
              sleep 2
            done

            # If DB not ready after wait, fail
            if ! docker-compose -f docker-compose.yml -f docker-compose.staging.resolved.yml exec -T db pg_isready -U postgres -d ZizaLend >/dev/null 2>&1; then
              echo "DB did not become ready"
              exit 1
            fi

            # Run migrations (must succeed)
            docker-compose -f docker-compose.yml -f docker-compose.staging.resolved.yml run --rm backend node node_modules/node-pg-migrate/bin/node-pg-migrate.js up

            # Start backend and frontend
            docker-compose -f docker-compose.yml -f docker-compose.staging.resolved.yml up -d backend frontend --remove-orphans

            # Health check: backend /health and frontend root
            for i in $(seq 1 24); do
              ok=0
              if curl -fsS http://localhost:3001/health >/dev/null 2>&1; then
                ok=$((ok+1))
              fi
              if curl -fsS http://localhost:3000/ >/dev/null 2>&1; then
                ok=$((ok+1))
              fi
              if [ "$ok" -eq 2 ]; then
                echo "Health checks passed"
                exit 0
              fi
              sleep 5
            done

            echo "Health check failed, attempting rollback using rollback compose"
            PREV_BACKEND="ghcr.io/${{ env.OWNER_LC }}/zizalend-backend:staging-previous"
            PREV_FRONTEND="ghcr.io/${{ env.OWNER_LC }}/zizalend-frontend:staging-previous"
            docker pull $PREV_BACKEND || true
            docker pull $PREV_FRONTEND || true
            docker-compose -f docker-compose.yml -f docker-compose.staging.rollback.yml up -d --remove-orphans
            exit 1
